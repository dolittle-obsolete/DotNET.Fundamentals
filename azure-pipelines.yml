trigger:
- master

resources:
  repositories:
    - repository: templates
      type: github
      name: dolittle-build-testing/AzureDevOps
      endpoint: dolittle-build-testing

jobs:
- template: Source/Test/pipeline.yml@templates
